version: '3.8'

services:
  api:
    build: .
    container_name: email_classifier
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    # Optional: Volume mapping for development (syncs code changes without rebuild)
    # volumes:
    #   - .:/app